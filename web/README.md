# 研面官 · 预推免物语（web）

这是「研面官」的前端 + 后端代码。项目介绍、产品说明、运行与部署方式见根目录 [`README.md`](../README.md)。

```bash
npm install
npm run dev   # http://localhost:3000
```

LLM 端点需通过环境变量 `ANTHROPIC_BASE_URL` / `ANTHROPIC_AUTH_TOKEN` 配置，详见根 README。
